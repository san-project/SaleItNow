import 'package:flutter/material.dart';

class SellerNotApprovedScreen extends StatelessWidget {
  const SellerNotApprovedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [
          Text(
            'Not Approved Yet',
          ),
        ],
      ),
    );
  }
}
